apiVersion: v1
kind: Service
metadata:
  name: project4
spec:
  selector:
    app: project4
  ports:
    - protocol: TCP
      port: 8090
      targetPort: 8090
      nodePort: 30080
  type: NodePort
  